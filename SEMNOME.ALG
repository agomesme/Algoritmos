algoritmo "soma"
var
 soma, soma1, nro, s: inteiro
 
inicio
      se(nro=100) entao
      s <- nro + soma
      
      senao
      s <- soma1(nro+1, nro+soma)

      
soma <- soma1(1,0)
Escreva("A Soma dos cem primeiros numeros é", soma)

fimse
fimalgoritmo